node_modules/
dist/
.vite/
.env
.env.local
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
coverage/
*.tsbuildinfo

# QA 산출물(크롬 프로파일·스크린샷·로그). 재생성 가능하고 용량이 커서 제외한다.
artifacts/
# 대화 로그 원본. API 키가 포함되어 있어 저장소에 올리지 않는다.
session-log/
# 런타임·브라우저 디버그 로그. API 키가 URL에 남고 인수인계에 쓸모가 없다.
.gstack/
.runtime-*.log

# 코드리뷰·디자인훅 산출물. 재생성 가능하고 저장소에 남길 것이 아니다.
# 연속지적도 인덱스 산출물. 390만 필지라 수 GB 까지 커진다.
# 원본 shapefile 로 언제든 재생성하므로 저장소에 두지 않는다.
# 인허가 원장 스냅샷(60MB). sync_facilities.py 로 재생성한다.
backend/data/facilities.db
backend/data/factory_geocode_cache.json
backend/data/cadastral_*.sqlite
backend/data/cadastral_*.db

# PNU 확정 캐시. 원천·법정동표·지적도 서명으로 자동 무효화되므로
# 언제든 재생성된다. 기동 시간만 줄이는 산출물이다.
backend/data/factory_pnu_resolution.*.json

# 정문 기준점 수기 지정. 운영 중 담당자가 만드는 값이라
# 저장소가 아니라 배포 환경에 남는다.
backend/data/front_doors.json

.claude-review/
.impeccable/

# 작업 메모. 문서는 PRD 와 판정 정본만 모은다.
docs/*
!docs/LH_DEMO_PRD.md
# 유해요소 판정의 계약서. 코드가 이 문서를 기준으로 동작하므로 함께 관리한다.
!docs/HAZARD_RULEBOOK_V1.4.md
# 데이터 원천 대조 감사. 어떤 원천이 왜 연결/미연결인지의 근거다.
!docs/DATA_SOURCE_AUDIT_260827.md
# LH 심사기준 시설군별 원천·연결 현황. 타 구현과 대조하는 기준표다.
!docs/LH_CRITERIA_COVERAGE.md
!docs/LH_CRITERIA_COVERAGE.html
# 심사 엔진 설명서. 무엇이 구현됐고 무엇이 남았는지의 단일 출처다.
!docs/SCREENING_ENGINE_GUIDE.md
# 유해요소 29종의 원천·판정기준 작업표. 한 항목씩 검토할 때의 단일 출처다.
!docs/HAZARD_SOURCE_MATRIX.md
# 회의·결정 단일 시간축. 무엇이 언제 정해졌는지의 유일한 출처다.
!docs/TIMELINE.md
# 측정기준 정본. 거리를 무엇에서 무엇까지 재는지의 전제다.
!docs/MEASUREMENT.md
# 유해요소별 판정 정리(A4 1장). LH 원본 조·목 단위로 하나씩 닫는다.
!docs/hazards/
!docs/hazards/**
# 유해요소 검증 증빙(좌표 대조 등). 문서의 수치를 재현할 수 있게 남긴다.
!docs/hazards/evidence/
!docs/hazards/evidence/**
# 작업 인계 문서. 세션 기록은 PC 로컬에만 남으므로 맥락을 저장소로 옮긴다.
!docs/HANDOFF_260827.md
!docs/HANDOFF_260830.md
!docs/HANDOFF_260911.md
!docs/HANDOFF_260913.md
# 좌표계·PNU·지적도 교재 + 프로젝트 브리핑. 새 세션·다른 LLM 에게 넘기는 입문서다.
!docs/COORDINATES_PRIMER_2026-09-13.md
# 결정 반영 전후 회귀 보고(9/13). 대용량 결과 JSON 은 제외하고 보고서·요약만 남긴다.
!docs/reports/
docs/reports/*
!docs/reports/regress_20260913/
docs/reports/regress_20260913/*
!docs/reports/regress_20260913/*.html
!docs/reports/regress_20260913/regress.json
# 프로젝트 연혁. 판정 규칙의 숫자가 왜 그 값인지의 근거와 결정 경위다.
!docs/PROJECT_TIMELINE.md
!docs/IESTATE_구현검증_종합보고_2026-08-28.html
# 실구동 보고. 같은 표본을 같은 조건으로 다시 돌릴 때의 대조 기준이다.
!docs/LH_API교차검증_실구동보고_2026-09-08.html
# 실구동 보고서에 실은 화면 캡처 원본. 보고서는 data URI 로 품고 있으나
# 원본을 함께 남겨야 다시 자르거나 확대해 볼 수 있다.
!docs/shots/
!docs/shots/**
# 근거자료 원본. 룰북·체크리스트·회신 내역이 있어야 판정 근거를 추적할 수 있다.
!docs/sources/
!docs/sources/**
# 단, 카카오톡 대화내역 원본은 제외한다. 주민번 앞자리·개인 이메일·계약금액 등
# 판정과 무관한 개인정보가 섞여 있고 git 히스토리는 되돌리기 어렵다.
docs/sources/*KakaoTalk*
docs/sources/*kakaotalk*

# 비밀키 사본 (backend/.env 백업·변형)
backend/*.env
backend/.env.bak.*
!backend/.env.example
# 미연결 원천 전국 공개 API 조사(2026-09-14). Grok·Codex 원문과 종합. 어떤 항목이 왜 API로 못 붙는지의 근거다.
!docs/API_SOURCE_RESEARCH_2026-09-14.md
!docs/research/
!docs/research/**
!docs/HANDOFF_260914.md
# 상용화 로드맵(2026-09-14). 코드 감사 근거로 고칠 것 전부와 우선순위·완료 기준.
!docs/PRODUCTION_ROADMAP_2026-09-14.md

# 관리자 「기준 편집」 저장값(로컬 설정)
backend/data/rule_overrides.json
